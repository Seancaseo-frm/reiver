# Copy to repo root as .env.mock and run: source .env.mock && make dev
# Or export these before starting Flow/website so the gateway uses the mock server (no real API keys or cost).
GATEWAY_OPENAI_BASE_URL=http://127.0.0.1:8090
GATEWAY_ANTHROPIC_BASE_URL=http://127.0.0.1:8091
GATEWAY_GOOGLE_BASE_URL=http://127.0.0.1:8092/v1beta
