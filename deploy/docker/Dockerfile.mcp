FROM debian:trixie-slim
RUN apt-get update && apt-get install -y \
    ca-certificates libssl3t64 \
    && rm -rf /var/lib/apt/lists/*
WORKDIR /app
COPY output/reiver-mcp .
EXPOSE 3002
CMD ["./reiver-mcp", "--transport", "http", "--listen", "0.0.0.0:3002"]
