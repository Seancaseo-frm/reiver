apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: reiver

resources:
  - namespace.yaml
  - watch-deployment.yaml
  - watch-workers-deployment.yaml
  - flow-deployment.yaml
  # Pond disabled — re-enable when Pond launches
  # - pond-deployment.yaml
  # - pond-workers-deployment.yaml
  - website-deployment.yaml
  - website-workers-deployment.yaml
  - docs-deployment.yaml
  - mcp-deployment.yaml
  - herd-deployment.yaml
  - otel-collector.yaml
  - pricing-sync-cronjob.yaml
