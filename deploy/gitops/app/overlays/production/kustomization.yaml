# GitOps app (production): delegates to existing Kustomize overlay.
# Argo CD syncs this path; CI updates image tags in ../../../k8s/overlays/production.
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - ../../../../k8s/overlays/production
