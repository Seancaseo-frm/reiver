apiVersion: v1
kind: Service
metadata:
  name: drone-kubernetes-secrets
  namespace: drone
  labels:
    app.kubernetes.io/name: drone-kubernetes-secrets
spec:
  type: ClusterIP
  ports:
    - port: 3000
      targetPort: 3000
      name: http
  selector:
    app.kubernetes.io/name: drone-kubernetes-secrets
