apiVersion: v1
kind: Service
metadata:
  name: drone-secret-debug-proxy
  namespace: drone
  labels:
    app: drone-secret-debug-proxy
spec:
  selector:
    app: drone-secret-debug-proxy
  ports:
    - port: 3000
      targetPort: 3000
      name: http
