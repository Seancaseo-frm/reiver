# Kanidm identity management server. Port 8443 (HTTPS only).
# Service: kanidm.reiver-infra.svc:8443
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: reiver-infra

resources:
  - pvc.yaml
  - configmap.yaml
  - certificate.yaml
  - deployment.yaml
  - service.yaml
