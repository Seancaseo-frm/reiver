# Loaded automatically for `npm run build` / `vite build` (production mode).
# Shell variables set in CI override these. For staging, set VITE_DOCS_BASE_URL in the pipeline.
VITE_DOCS_BASE_URL=https://docs.reiver.ai

# Optional: public Calendly link (opens in new tab). Unset => mailto fallback on marketing pages.
# VITE_CALENDLY_URL=
